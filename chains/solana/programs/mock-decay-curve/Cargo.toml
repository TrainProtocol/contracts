[package]
name = "mock-decay-curve"
version = "0.1.0"
description = "Configurable test payout curve for the Train HTLC (testing only)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "mock_decay_curve"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.32.1"
